import React from 'react';
import PropTypes from 'prop-types';
import './styles.css'
Header.propTypes = {

};

function Header(props) {
    return (
        <div>

        </div>
    );
}

export default Header;